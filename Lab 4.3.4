#Lab 4.3.4
#Write a function which takes one argument (a year) and returns True of it is a leap year or False otherwise

def is_year_leap(year):
    if year % 4 != 0: #copied and pasted from the calendar lab
        return False #used return keyword to display True or False
    elif year % 100 != 0: #copied and pasted from the calendar lab
        return True #used return keyword to display True or False
    elif year % 400 != 0: #copied and pasted from the calendar lab
        return False #used return keyword to display True or False
    else: #if nothing matches, it is True (copied from calendar lab)
        return True 

year1 = int(input("Enter a year: ")) #input 1900        
print(is_year_leap(year1)) #input value defined by the variable = argument which is passed to be the parameter
year2 = int(input("Enter a year: ")) #input 2000  
print(is_year_leap(year2))
year3 = int(input("Enter a year: ")) #input 2016
print(is_year_leap(year3))
year4 = int(input("Enter a year: ")) #input 1987 
print(is_year_leap(year4))

#Solution
#def is_year_leap(year):
    #if year % 4 != 0:
        #return False
    #elif year % 100 != 0:
        #return True
    #elif year % 400 != 0:
        #return False
    #else:
        #return True

#test_data = [1900, 2000, 2016, 1987]
#test_results = [False, True, True, False]
#for i in range(len(test_data)):
   #yr = test_data[i]
   #print(yr,"-> ",end="")
    #result = is_year_leap(yr)
    #if result == test_results[i]:
        #print("OK")
    #else:
        #print("Failed")

