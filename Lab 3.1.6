n = int(input("Enter a number: "))
#defines a variable n and assigns the value input by the user
#input value is also converted to an integer

print(n >= 100)
#this satisfies both conditions
#if n >= 100, returns True (condition 2)
#if n is not >= 100, it must be < 100, so returns False (condition 1)
